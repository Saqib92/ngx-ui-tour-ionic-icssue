import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ngx-ui-tour-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
